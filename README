To Install and Run:

    1. Install go.
    2. Run the following commands from the current directory:
       $ source master.sh
       $ install
    3. Run any of the other displayed commands.

--------

Available Commands:

    install         - download go dependencies
    restart_server  - run a server; stop any running server first
    run_tests       - run all of the tests